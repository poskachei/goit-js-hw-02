'use strict';

let message = prompt(`Введите число:`);
let input = message;

if (input !== null) {
  const number = [];
  let newNumber = number.push(input);
} else {
  let total = 0;
  total += input;
  alert('Общая сумма чисел равна ' + total);
}
// Не доработано
